import{_ as a,c as t,o,ag as r}from"./chunks/framework.BpVvFBTM.js";const m=JSON.parse('{"title":"Damage","description":"","frontmatter":{},"headers":[],"relativePath":"vitals and damage/damage.md","filePath":"vitals and damage/damage.md"}'),s={name:"vitals and damage/damage.md"};function d(l,e,n,i,p,h){return o(),t("div",null,[...e[0]||(e[0]=[r(`<h1 id="damage" tabindex="-1">Damage <a class="header-anchor" href="#damage" aria-label="Permalink to &quot;Damage&quot;">​</a></h1><p>The damage component is a compact inline calculator that helps you apply damage using Daggerheart’s tiered damage rules. It:</p><p>• Shows Minor / Major / Severe thresholds.<br> • Lets you enter a raw damage amount and number of Armor slots used.<br> • Highlights the resulting damage tier after Armor reduction.<br> • Applies the result directly to your HP and Armor trackers, using shared state keys.</p><p>You can use it for:</p><p>• Fast damage application during combat.<br> • Central “GM panel” that updates a character’s HP/Armor from one place.<br> • Player‑facing widgets in character sheets to quickly mark damage.</p><blockquote><p>The damage block does not render HP/Armor trackers itself; it talks to existing trackers (from vitals or individual hp/armor blocks) using shared keys.</p></blockquote><p>How thresholds and level work</p><p>The block needs Major and Severe thresholds to know when damage changes tiers.</p><p>Thresholds come from:</p><ol><li>YAML options (damage block)<br> ◦ major_threshold<br> ◦ severe_threshold</li><li>Frontmatter fields on the note (if the YAML options are omitted), such as major_threshold, severe_threshold, or similar aliases.</li><li>Base values plus level<br> ◦ base_major and base_severe define raw thresholds.<br> ◦ level (from the block or frontmatter level / tier) is added to those thresholds.</li></ol><p>The plugin automatically adds your level to the chosen source thresholds, so you don’t have to pre‑bake level into your numbers.</p><p>Dynamic thresholds with templates</p><p>major_threshold and severe_threshold support template values with <span>{{ }}</span>, processed like other blocks. This lets you base thresholds on frontmatter or other template expressions, for example:</p><p>• Pull thresholds directly from frontmatter fields.<br> • Compute thresholds from a formula (e.g. “base + tier”).</p><blockquote><p>level itself is read as a number from the block or from frontmatter (level / tier); it is not templated.</p></blockquote><h2 id="example-–-simple-damage-panel" tabindex="-1">Example – Simple damage panel <a class="header-anchor" href="#example-–-simple-damage-panel" aria-label="Permalink to &quot;Example – Simple damage panel&quot;">​</a></h2><div class="language-damage vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">damage</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>styleClass: </span></span>
<span class="line"><span></span></span>
<span class="line"><span># Optional: override which HP and Armor trackers this block affects.</span></span>
<span class="line"><span># If omitted, HP uses the default &quot;din_health&quot; key, and Armor</span></span>
<span class="line"><span># is note-scoped by default.</span></span>
<span class="line"><span>hp_key: &quot;din_health&quot;</span></span>
<span class="line"><span>armor_key: &quot;din_armor::character-sheet&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Direct numeric thresholds (before level is added).</span></span>
<span class="line"><span>base_major: 4</span></span>
<span class="line"><span>base_severe: 8</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Level can come from frontmatter instead; this is optional.</span></span>
<span class="line"><span>level: 3</span></span></code></pre></div><p>This will:</p><ul><li>Compute <strong>Major</strong> threshold as <code>4 + level</code> (here, 7).</li><li>Compute <strong>Severe</strong> threshold as <code>8 + level</code> (here, 11).</li><li>Let you enter a damage amount and how many Armor slots were used.</li><li>Highlight the final tier (Minor / Major / Severe) after Armor reduction.</li><li>When you click <strong>Apply</strong>, it updates HP and Armor via the given keys and shows a notice with the result.</li></ul><p>The plugin will:</p><ul><li>Read <code>major_threshold</code> and <code>severe_threshold</code> from frontmatter.</li><li>Read <code>level</code> from frontmatter.</li><li>Add <code>level</code> to both thresholds and display the final values in the step diagram.</li></ul><h2 id="hp-armor-integration" tabindex="-1">HP &amp; Armor integration <a class="header-anchor" href="#hp-armor-integration" aria-label="Permalink to &quot;HP &amp; Armor integration&quot;">​</a></h2><p>The block updates trackers by writing to the same state keys that HP and Armor trackers use:</p><ul><li><p><strong><code>hp_key</code></strong></p><ul><li>Default: a shared HP key (e.g. <code>&quot;din_health&quot;</code>) compatible with the <code>vitals</code> / <code>hp</code> blocks.</li><li>Set this explicitly if you want separate HP pools or a custom naming scheme.</li></ul></li><li><p><strong><code>armor_key</code></strong></p><ul><li>Default: a note‑scoped Armor key like <code>&quot;din_armor::&amp;lt;note-path&amp;gt;&quot;</code>.</li><li>Set this if you want a shared Armor pool across multiple notes or characters.</li></ul></li></ul><p>When you click <strong>Apply</strong>, the damage calculator:</p><ul><li>Determines the damage tier (Minor / Major / Severe).</li><li>Applies the appropriate HP marks and Armor usage via the configured keys.</li><li>Shows a short message indicating what changed.</li></ul><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Top‑level <code>damage</code> block options:</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>styleClass</code></td><td>String</td><td><em>none</em></td><td>Optional CSS class applied to the damage block container (preferred styling hook).</td></tr><tr><td><code>hp_key</code></td><td>String</td><td><code>&quot;din_health&quot;</code></td><td>State key for the HP tracker this damage block should update.</td></tr><tr><td><code>armor_key</code></td><td>String</td><td><code>din_armor::(note)</code></td><td>State key for the Armor tracker this damage block should update (defaults to note‑scoped).</td></tr><tr><td><code>major_threshold</code></td><td>Number / String</td><td><em>from FM / base</em>*_</td><td>Major damage threshold; can be a number or a template.</td></tr><tr><td><code>severe_threshold</code></td><td>Number / String</td><td><em>from FM / base</em>*_</td><td>Severe damage threshold; can be a number or a template.</td></tr><tr><td><code>base_major</code></td><td>Number / String</td><td><code>0</code></td><td>Base major threshold used when <code>major_threshold</code> / frontmatter values are not set.</td></tr><tr><td><code>base_severe</code></td><td>Number / String</td><td><code>0</code></td><td>Base severe threshold used when <code>severe_threshold</code> / frontmatter values are not set.</td></tr><tr><td><code>level</code></td><td>Number</td><td>from FM <code>level</code> / <code>tier</code> or <code>0</code></td><td>Level added to major and severe thresholds for final values.</td></tr></tbody></table><p>This makes the <code>damage</code> block a fast, rules‑aware control center for applying damage that stays in sync with your existing HP and Armor trackers.</p>`,30)])])}const u=a(s,[["render",d]]);export{m as __pageData,u as default};
