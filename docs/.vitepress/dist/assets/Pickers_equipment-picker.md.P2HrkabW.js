import{_ as t,c as o,o as i,ag as a}from"./chunks/framework.BpVvFBTM.js";const h=JSON.parse('{"title":"Equipment Picker","description":"","frontmatter":{},"headers":[],"relativePath":"Pickers/equipment-picker.md","filePath":"Pickers/equipment-picker.md"}'),r={name:"Pickers/equipment-picker.md"};function d(n,e,s,c,l,p){return i(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="equipment-picker" tabindex="-1">Equipment Picker <a class="header-anchor" href="#equipment-picker" aria-label="Permalink to &quot;Equipment Picker&quot;">​</a></h1><p>The equipment picker is a management UI for weapons, armor, and other gear. It lets you move items between two lists on a character note:</p><ul><li><strong>Inventory</strong> – everything your character owns.</li><li><strong>Equipped</strong> – what they are currently carrying/using.</li></ul><p>It integrates with:</p><ul><li>Your <strong>equipment notes</strong> (weapons, armor, etc.), discovered via tags, folders, or plugin settings.</li><li>Your <strong>character note frontmatter</strong> (<code>tier</code>, <code>inventory</code>, <code>equipped</code>).</li><li>The <strong>Dataview</strong> plugin.</li></ul><blockquote><p>The Equipment Picker requires Dataview. If Dataview is missing, the block shows a message instead of rendering.</p></blockquote><h2 id="character-frontmatter-it-uses" tabindex="-1">Character frontmatter it uses <a class="header-anchor" href="#character-frontmatter-it-uses" aria-label="Permalink to &quot;Character frontmatter it uses&quot;">​</a></h2><p>On the character note (the one containing the <code>equipmentpicker</code> block), the picker reads:</p><ul><li><code>tier</code> / <code>Tier</code> – character tier, used to optionally hide items above that tier.</li><li><code>inventory</code> / <code>Inventory</code> – list of equipment notes (as <code>[[links]]</code>).</li><li><code>equipped</code> / <code>Equipped</code> – list of currently equipped items (as <code>[[links]]</code>).</li></ul><p><code>inventory</code> and <code>equipped</code> are automatically updated when you click <strong>Add / Remove / → Inventory / → Equipped</strong> in the UI.</p><h2 id="equipment-notes-it-expects" tabindex="-1">Equipment notes it expects <a class="header-anchor" href="#equipment-notes-it-expects" aria-label="Permalink to &quot;Equipment notes it expects&quot;">​</a></h2><p>Each equipment item is just a note in your vault. The picker finds them by:</p><ul><li>Block‑level <code>folder</code> / <code>folders</code> values, or</li><li>The plugin‑wide <strong>Equipment folder</strong> setting, or</li><li>As a fallback, any note detected as equipment via tags/fields.</li></ul><p>An equipment note is considered a candidate when:</p><ul><li><code>category</code> / <code>Category</code> / <code>type</code> / <code>Type</code> contains &quot;weapon&quot;, &quot;armor&quot;, or &quot;equipment&quot;, <strong>or</strong></li><li>The note has tags like <code>#equipment</code>, <code>#weapon</code>, <code>#armor</code>, <strong>or</strong></li><li>It has obvious combat fields like <code>damage</code>, <code>thresholds</code>, <code>base_score</code>, <code>tier</code>, etc.</li></ul><p>From each item it reads fields such as:</p><ul><li><code>category</code> / <code>Category</code> – high‑level category (often &quot;Weapon&quot; or &quot;Armor&quot;).</li><li><code>type</code> / <code>Type</code> – sub‑type or style.</li><li><code>damage</code> / <code>Damage</code> – damage string.</li><li><code>thresholds</code> / <code>Thresholds</code> – armor/threshold info.</li><li><code>base_score</code> / <code>base</code> / <code>Base</code> – base score.</li><li><code>tier</code> / <code>Tier</code> – item tier.</li><li><code>trait</code> / <code>Trait</code>, <code>range</code> / <code>Range</code>, <code>burden</code> / <code>Burden</code>, <code>feature</code> / <code>Feature</code>.</li></ul><p>These are shown in the Inventory/Equipped tables and in the Add Equipment modal.</p><h2 id="basic-usage" tabindex="-1">Basic usage <a class="header-anchor" href="#basic-usage" aria-label="Permalink to &quot;Basic usage&quot;">​</a></h2><div class="language-equipmentpicker vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">equipmentpicker</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># optional per-block overrides</span></span>
<span class="line"><span># folders:</span></span>
<span class="line"><span>#   - &quot;Cards/Equipment&quot;</span></span>
<span class="line"><span># enforce_tier: true   # default: use character tier to hide too-high-tier items</span></span>
<span class="line"><span># view: table          # or &quot;card&quot; for card-style tiles</span></span></code></pre></div><h2 id="ui-overview" tabindex="-1">UI overview <a class="header-anchor" href="#ui-overview" aria-label="Permalink to &quot;UI overview&quot;">​</a></h2><h3 id="main-toolbar" tabindex="-1">Main toolbar <a class="header-anchor" href="#main-toolbar" aria-label="Permalink to &quot;Main toolbar&quot;">​</a></h3><ul><li><strong>Equipped / Inventory</strong> buttons – toggle which list you’re looking at.</li><li><strong>Add equipment</strong> – opens the Add Equipment modal.</li></ul><h3 id="inventory-equipped-tables" tabindex="-1">Inventory / Equipped tables <a class="header-anchor" href="#inventory-equipped-tables" aria-label="Permalink to &quot;Inventory / Equipped tables&quot;">​</a></h3><p>For the active list, the picker shows:</p><ul><li>Separate sections for <strong>Weapons</strong>, <strong>Armor</strong>, and <strong>Other</strong> (based on category/tags).</li><li>Tables with columns like Name, Type, Damage, Thresholds, Base, Tier, Trait, Range, Burden, Feature.</li><li>Per‑row <strong>Actions</strong>: <ul><li><code>→ inventory</code> / <code>→ equipped</code> – move items between lists.</li><li><strong>Remove</strong> – remove an item from that list.</li></ul></li></ul><h3 id="add-equipment-modal" tabindex="-1">Add Equipment modal <a class="header-anchor" href="#add-equipment-modal" aria-label="Permalink to &quot;Add Equipment modal&quot;">​</a></h3><p>The modal lets you search and add equipment to Inventory or Equipped. It supports:</p><ul><li><strong>Category filter</strong> – Weapon / Armor / Other.</li><li><strong>Tier filter</strong> – Any or exact tier.</li><li><strong>Search</strong> – text search over name and properties/tags.</li><li>Two view modes: <ul><li><code>card</code> – grid of cards with art and summary lines.</li><li><code>table</code> – compact table with all core fields.</li></ul></li></ul><p>From here you can add items directly to:</p><ul><li><strong>Inventory</strong> – appends a link to the character’s <code>inventory</code> list.</li><li><strong>Equipped</strong> – appends a link to <code>equipped</code> (and ensures the same item isn’t in both lists).</li></ul><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Top‑level <code>equipmentpicker</code> block options:</p><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>folder</code></td><td>String / String array</td><td><em>none</em></td><td>Limit search to a folder or list of folders. Uses forward slashes.</td></tr><tr><td><code>folders</code></td><td>String / String array</td><td><em>none</em></td><td>Alias for <code>folder</code>; if both are present, <code>folders</code> wins.</td></tr><tr><td><code>enforce_tier</code></td><td>Boolean</td><td>plugin setting</td><td>When true, hide items with <code>tier</code> above the character’s tier.</td></tr><tr><td><code>view</code></td><td><code>&quot;card&quot;</code> | <code>&quot;table&quot;</code></td><td><code>&quot;card&quot;</code></td><td>View type for the Add Equipment modal.</td></tr></tbody></table><p>Behavior notes:</p><ul><li>If <code>folder</code>/<code>folders</code> is set, only equipment under those folders is considered.</li><li>If no folders are configured, the plugin falls back to the global Equipment folder, then to a vault‑wide search.</li><li>When <code>enforce_tier</code> is enabled, items with <code>tier</code> higher than the character’s <code>tier</code> are hidden in the Add Equipment modal.</li><li>The picker keeps <code>inventory</code> and <code>equipped</code> mutually exclusive for each item (moving to one removes it from the other).</li></ul><p>This makes the Equipment Picker a handy way to keep your character’s gear organized without editing frontmatter arrays by hand.</p>`,37)])])}const m=t(r,[["render",d]]);export{h as __pageData,m as default};
